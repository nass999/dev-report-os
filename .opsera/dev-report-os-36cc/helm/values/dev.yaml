# .opsera/dev-report-os-36cc/helm/values/dev.yaml
# Auto-generated by Opsera Vibe-Shift on first deploy to dev.
# Non-secret vars detected from repo. Edit freely — applied on next deploy.
# Secret vars (marked PORTAL ONLY) must be configured in Opsera portal → Variables tab.

replicaCount: 1
resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "4Gi"
    cpu: "2000m"
ingress:
  className: nginx
  customDomain: ""

env:
  NODE_ENV: "production"
  PORT: "5174"
  APP_ENV: "dev"

  # ── Configure these secrets in Opsera portal → Variables tab ──
  # SESSION_SECRET: ""  ← PORTAL ONLY (secret)
